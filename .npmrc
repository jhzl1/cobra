# HeroUI ships its Tailwind theme as transitive @heroui/* packages. Without the
# hoist, Tailwind's @source scan cannot reach them under pnpm's nested store and
# every HeroUI component renders unstyled.
public-hoist-pattern[]=*@heroui/*
